module.exports = (api, options = {}, rootOptions = {}) => {
    api.injectImports(api.entryFile, `import '@/assets/js/websocket'`)

    api.extendPackage({
        dependencies: {
            "vue-native-websocket": "^2.0.14",
        }
    })
    api.render('./template', {})
}

